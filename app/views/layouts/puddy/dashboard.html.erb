<!DOCTYPE html>
<html>
<head>
  <title>Puddy</title>
  <%= stylesheet_link_tag "puddy/application", "application", "admin", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<header>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <%= link_to 'Camp Registration Systems Dashboard',
          root_path, class: 'brand' %>

        <ul class="nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Transactions
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to "All transactions", puddy.transactions_path %>
              </li>
              <li>
                <%= link_to "Successful transactions",
                  puddy.successful_transactions_path %>
              </li>
              <li>
                <%= link_to "Failed transactions",
                  puddy.failed_transactions_path %>
              </li>
            </ul>
        </ul>
      </div>
    </div>
  </div>
</header>

<div class="container-fluid">
  <div class="row-fluid">
    <div class="span4 well sidebar">
      <ul>
        <% accounts.each do |account| %>
          <li><%= link_to account.name, account %></li>
        <% end %>
      </ul>
    </div>

    <div class="span8 well main-content">
      <%= yield%>
    </div>
  </div>
</div>

</body>
</html>
